cd backend
PYTHONPATH=. uvicorn app.main:app --reload --host 0.0.0.0 --port 8000


cd bot
python main.py


cd frontend
npm run dev


git pull
# після змін backend (AUTO.RIA) або frontend (API URL, PWA):
# без --no-cache — BuildKit використовує кеш; ліміт на сервері: scripts/setup-docker-cache-limits.sh
docker compose build backend frontend worker
docker compose up -d backend frontend worker
# або: ./scripts/docker-deploy.sh backend frontend
# перевірка: curl -s http://localhost:8000/health
# у .env для production: NEXT_PUBLIC_API_URL=/api/v1  (НЕ http://backend:8000)
# якщо кабінет падає до F5 — старий Service Worker; після rebuild один раз hard refresh (Cmd+Shift+R)


# 1. Зупинити контейнери
docker compose down

# 2. Перезібрати frontend
docker compose build frontend

# 3. Запустити заново
docker compose up -d


git add .
git commit -m "some updates"
git push