══════════════════════════════════════════════════════════════════════════════
  CARBIT — локальний запуск і деплой
══════════════════════════════════════════════════════════════════════════════

─── 1. Локально (3 термінали) ───────────────────────────────────────────────

# Термінал 1 — Backend
source .venv/bin/activate
cd backend
PYTHONPATH=. alembic upgrade head   # один раз після git pull, якщо «no such table»
PYTHONPATH=. python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Термінал 2 — Telegram-бот (привʼязка акаунта, сповіщення)
source .venv/bin/activate
cd bot
python main.py

# Термінал 3 — Frontend
cd frontend
npm run dev

  Frontend   http://localhost:3000
  API docs   http://localhost:8000/api/docs
  Admin      http://localhost:3000/admin  (admin / admin123 за замовч.)

─── 2. Новий функціонал (коротко) ───────────────────────────────────────────

▸ Вхід / реєстрація — ТІЛЬКИ номер телефону
  • Реєстрація → SMS-код (TurboSMS, відправник Carbit)
  • Вхід → код у Telegram, якщо акаунт привʼязаний до бота
  • Якщо Telegram недоступний → SMS (кнопка «Надіслати SMS»)
  • Повторна відправка коду — не частіше 1 раз на хвилину
  • Email / Google / вхід через бота на сторінці login — вимкнено в UI

▸ Голосовий пошук (AI)
  • Тільки в кабінеті: /app/search, /app/dashboard
  • На лендінгу кнопка «Голосом» показує підказку «доступно в кабінеті»
  • Потрібен OPENAI_API_KEY у .env
  • Запис зупиняється після 5 сек тиші → AI заповнює фільтри → «Шукати»

▸ Порівняння без входу
  • /app/compare?ids=olx_... — працює для гостей

▸ Telegram у кабінеті
  • Налаштування → привʼязати Telegram (сповіщення + коди входу)
  • Бот: TELEGRAM_BOT_TOKEN + TELEGRAM_BOT_USERNAME у .env

▸ Публічне порівняння / Instagram / інше
  • Див. git log і frontend/src/components/social/

─── 3. .env — обовʼязкові змінні для нових фіч ───────────────────────────────

# ── TurboSMS (реєстрація + SMS-fallback при вході) ──
turbo_sms_token=...          # або TURBOSMS_TOKEN=...
TURBOSMS_SENDER=Carbit       # має бути АКТИВОВАНИЙ у кабінеті turbosms.ua
                             # помилка NOT_ALLOWED_MESSAGE_SENDER = sender не схвалений

# ── OpenAI (голосовий пошук у кабінеті) ──
OPENAI_API_KEY=sk-...

# ── Telegram-бот ──
TELEGRAM_BOT_TOKEN=...
TELEGRAM_BOT_USERNAME=Car_Bit_Bot
INTERNAL_API_SECRET=...      # однаковий у backend і bot

# ── Загальне ──
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=/api/v1
DEBUG=true                   # локально; на проді DEBUG=false

# KV / сесії (локально SQLite, без окремого Redis)
REDIS_URL=sqlite://database/kv.db

─── 4. Git / швидкий деплой локально ────────────────────────────────────────

git pull
git add .
git commit -m "some updates"
git push

# Docker (локально або на VPS)
docker compose build backend frontend worker
docker compose up -d backend frontend worker
# або: ./scripts/docker-deploy.sh backend frontend
# curl -s http://localhost:8000/health

# NEXT_PUBLIC_API_URL змінюється під час BUILD frontend — після зміни потрібен rebuild
# Старий UI → hard refresh (Cmd+Shift+R) через Service Worker

docker compose down
docker compose build frontend
docker compose up -d


══════════════════════════════════════════════════════════════════════════════
  PRODUCTION — оновлення carbit.info (або інший VPS)
══════════════════════════════════════════════════════════════════════════════

─── 1. Підключення ───────────────────────────────────────────────────────────

ssh user@your-server
cd /path/to/carbit


─── 2. Оновити код ───────────────────────────────────────────────────────────

git pull
# локальні зміни на сервері: git stash  або  git checkout .


─── 3. .env на production ────────────────────────────────────────────────────

FRONTEND_URL=https://carbit.info
NEXT_PUBLIC_API_URL=/api/v1
DEBUG=false
SECRET_KEY=...               # python3 -c "import secrets; print(secrets.token_urlsafe(32))"

# Auth / SMS
TURBOSMS_TOKEN=...
TURBOSMS_SENDER=Carbit

# Голосовий пошук
OPENAI_API_KEY=...

# Telegram
TELEGRAM_BOT_TOKEN=...
TELEGRAM_BOT_USERNAME=Car_Bit_Bot
INTERNAL_API_SECRET=...

# OAuth (legacy API, UI вимкнено — можна лишити для старих акаунтів)
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
GOOGLE_REDIRECT_URI=https://carbit.info/api/v1/auth/google/callback

# Email (Resend — привʼязка email у кабінеті, не для login)
RESEND_API_KEY=...

# Telegram parser
TELEGRAM_ENABLED=true
TELETHON_API_ID=...
TELETHON_API_HASH=...
TELETHON_NUMBER=+380...
TELETHON_SESSION_NAME=carbit_parser
# Канали — в адмінці /admin/channels

Важливо:
• NEXT_PUBLIC_API_URL — build-time для frontend → після зміни rebuild frontend
• BACKEND_URL для browser — публічний URL; bot/worker у Docker отримують backend:8000 з compose
• НЕ використовуй http://backend:8000 у NEXT_PUBLIC_API_URL


─── 4. Папки на хості ───────────────────────────────────────────────────────

mkdir -p database media

  database/  — autoradar.db, kv.db, Telethon *.session
  media/     — фото з Telegram-каналів

Telethon-сесія (один раз):
  python auth.py  →  database/<TELETHON_SESSION_NAME>.session
  Локально і VPS — РІЗНІ TELETHON_SESSION_NAME (AuthKeyDuplicatedError)


─── 5. Rebuild ────────────────────────────────────────────────────────────────

# Рекомендовано
./scripts/docker-deploy.sh

# Вручну — усі сервіси
docker compose build backend bot worker telegram-worker frontend
docker compose up -d

# Частково
./scripts/docker-deploy.sh backend bot worker telegram-worker   # Python
./scripts/docker-deploy.sh frontend                           # UI / PWA

# Не використовуй --no-cache без потреби


─── 6. Перевірка ─────────────────────────────────────────────────────────────

docker compose ps
curl -s http://localhost:8000/health
curl -I http://localhost:3000

docker compose logs -f backend --tail=50
docker compose logs -f worker --tail=50
docker compose logs -f telegram-worker --tail=50
docker compose logs -f bot --tail=30

У backend при старті: migrations → Starting backend...
Alembic на Docker стартує автоматично.


─── 7. Nginx ──────────────────────────────────────────────────────────────────

  /        → http://127.0.0.1:3000
  /api/    → http://127.0.0.1:8000/api/
  /api/v1/telegram-media/  → backend (фото в сповіщеннях)

sudo nginx -t && sudo systemctl reload nginx


─── 8. Після оновлення frontend ───────────────────────────────────────────────

Hard refresh у браузері (Cmd+Shift+R / Ctrl+Shift+R)


─── 9. Що перебілдити ────────────────────────────────────────────────────────

| Змінилось                         | Команда                                              |
|-----------------------------------|------------------------------------------------------|
| Повний реліз                      | ./scripts/docker-deploy.sh                           |
| Backend, parser, worker, bot      | ./scripts/docker-deploy.sh backend bot worker telegram-worker |
| Frontend                          | ./scripts/docker-deploy.sh frontend                  |
| Тільки runtime .env               | docker compose up -d  або  restart worker           |
| NEXT_PUBLIC_API_URL               | build frontend + up -d frontend                      |


─── 10. Бекап перед великим оновленням ────────────────────────────────────────

docker compose stop backend bot worker telegram-worker
cp database/autoradar.db "database/autoradar.db.bak.$(date +%F)"
cp database/kv.db "database/kv.db.bak.$(date +%F)"
docker compose start backend bot worker telegram-worker


─── 11. Типові проблеми ──────────────────────────────────────────────────────

| Симптом                              | Рішення                                           |
|--------------------------------------|---------------------------------------------------|
| NOT_ALLOWED_MESSAGE_SENDER           | Активуй sender «Carbit» у turbosms.ua             |
| 401 після реєстрації / входу         | Перезапусти backend; kv.db має підтримку zadd   |
| SMS не приходить                     | TURBOSMS_TOKEN, баланс TurboSMS, sender активний  |
| Код входу не в Telegram              | Привʼязати Telegram у кабінеті; fallback — SMS   |
| Голосовий пошук не працює            | OPENAI_API_KEY; тільки в /app, не на лендінгу     |
| telegram-worker AuthKeyDuplicated    | Один .session на VPS; /admin/channels → скинути   |
| 0 оголошень Telegram                 | Канали в /admin/channels, TELEGRAM_ENABLED=true   |
| Старий UI                            | rebuild frontend + hard refresh                   |
| no such table                        | alembic upgrade head (локально або в Docker logs) |
| backend unhealthy / dependency failed | docker logs carbit-backend --tail 80; див. §14   |


─── 14. backend unhealthy (docker compose up) ─────────────────────────────────

Діагностика:
  docker ps -a | grep backend
  docker logs carbit-backend --tail 100

Типові причини:
  • Порт 8000 зайнятий → lsof -i :8000 або змінити BACKEND_PORT у .env
  • Диск заповнений (media/) → df -h
  • KV_URL=redis://... але Redis недоступний → у .env:
      KV_URL=sqlite://database/kv.db
      REDIS_URL=sqlite://database/kv.db
    (або docker compose --profile cache up -d)
  • SQLite locked → docker compose stop; sleep 2; docker compose up -d backend
  • Помилка міграції → docker compose run --rm --entrypoint alembic backend upgrade head

Після git pull з /health/live:
  docker compose build backend && docker compose up -d backend
  curl -s http://localhost:8000/health/live


─── 12. Telethon-сесія ───────────────────────────────────────────────────────

Файл: database/<TELETHON_SESSION_NAME>.session

Адмінка: /admin/channels → Telethon → скинути → код → up telegram-worker

CLI на VPS:
  docker compose stop telegram-worker
  docker compose run --rm -it -w /app --entrypoint python telegram-worker auth.py --reset
  docker compose up -d telegram-worker


─── 13. Telegram media (диск) ────────────────────────────────────────────────

За замовчуванням: 1 фото на оголошення, JPEG ~1280px, авто-видалення за строком
зберігання (LISTING_MAX_AGE_DAYS, типово 120 днів ≈ 4 місяці).

.env:
  TELEGRAM_MAX_PHOTOS=1
  TELEGRAM_MEDIA_MAX_WIDTH=1280
  TELEGRAM_MEDIA_JPEG_QUALITY=82
  LISTING_MAX_AGE_DAYS=120

Разове очищення / стиснення на VPS:

  # 1) Перебілд backend (pillow + новий скрипт у образі)
  docker compose build backend telegram-worker
  docker compose up -d backend telegram-worker

  # 2) Dry-run
  docker compose run --rm --entrypoint python backend \
    backend/scripts/purge_telegram_media.py --dry-run --purge-old --compress

  # 3) Виконати
  docker compose run --rm --entrypoint python backend \
    backend/scripts/purge_telegram_media.py --purge-old --compress

  # Без Docker (якщо є .venv на хості):
  PYTHONPATH=backend:. python backend/scripts/purge_telegram_media.py --purge-old --compress

Автоматично:
  • telegram-worker — щогодини: видалення оголошень поза строком зберігання
    (усі джерела: telegram, olx, auto_ria) + їх .jpg з media/
  • кожен цикл парсера — те саме, перед парсингом
  • Telegram не сканує пости, старші за LISTING_MAX_AGE_DAYS


─── Один рядок — повний деплой ───────────────────────────────────────────────

cd /path/to/carbit && mkdir -p database media && ./scripts/docker-deploy.sh
