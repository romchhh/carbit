cd backend
PYTHONPATH=. uvicorn app.main:app --reload --host 0.0.0.0 --port 8000


cd bot
python main.py


cd frontend
npm run dev


git pull
# після змін backend (AUTO.RIA) або frontend (API URL):
docker compose up -d --build backend frontend
# перевірка AUTO.RIA: curl -s http://localhost:8000/health | grep auto_ria
# у .env для production: NEXT_PUBLIC_API_URL=/api/v1  (НЕ http://backend:8000)


# 1. Зупинити контейнери
docker compose down

# 2. Перезібрати frontend
docker compose build frontend

# 3. Запустити заново
docker compose up -d


git add .
git commit -m "some updates"
git push